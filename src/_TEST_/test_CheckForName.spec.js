/**
 * Jest test checkForName Function
 */

import { checkForName } from "../client/js/nameChecker";
describe("Test submit function", () => {
  test("test checkURL function", () => {
    expect(checkForName).toBeDefined();
  });
});
